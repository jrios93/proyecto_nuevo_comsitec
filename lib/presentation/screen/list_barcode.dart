import 'package:flutter/material.dart';

class ListBarcode extends StatelessWidget {
  const ListBarcode({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
